或 Postgres 密码认证失败。

**根因**：
- `testcontainers` 默认镜像的 MySQL 和 PostgreSQL 使用随机密码或未设置密码，而测试 URL 中硬编码了 `root:root`、`test:test`。
- 容器启动时没有通过环境变量指定密码与用户。

**修复**：
- 使用 `RunnableImage` 显示设置容器环境变量：
  ```rust
  RunnableImage::from(Mysql::default())
      .with_env_var(("MYSQL_ROOT_PASSWORD", "root"))
  ```
- 对于 PostgreSQL：
  ```rust
  RunnableImage::from(Postgres::default())
      .with_env_var(("POSTGRES_USER", "test"))
      .with_env_var(("POSTGRES_PASSWORD", "test"))
      .with_env_var(("POSTGRES_DB", "test"))
  ```
- 确保 `mysql_url` / `postgres_url` 与容器设定的凭证一致。

## 3. 查询返回整数值触发断言失败 `None != Some("1")`

**现象**：
