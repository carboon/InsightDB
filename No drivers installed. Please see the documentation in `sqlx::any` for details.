
**根因**：
- `sqlx` crate 未启用 `any` feature，同时也未在测试入口调用 `sqlx::any::install_default_drivers()`。
- `Cargo.toml` 中 `sqlx` 只包含了 `runtime-tokio-rustls` 等 feature，缺少 `any`。

**修复**：
1. 在 `Cargo.toml` 的 `sqlx` 依赖中加入 `"any"` feature。
2. 在每条需要数据库连接的测试开头添加：
   ```rust
   sqlx::any::install_default_drivers();
   ```
   该调用幂等，多次重复无副作用。

## 2. MySQL/PostgreSQL 容器认证失败

**现象**：
