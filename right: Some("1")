
**根因**：
- `DatabaseConnection::query` 返回的 `QueryResult` 中 `rows` 类型为 `Vec<Vec<Option<String>>>`，期望所有列值为字符串。
- 但 SQL 中 `SELECT 1 AS val` 返回的是整数，驱动将其表示为 `Value::Int`，在转换为字符串时失败，变成 `None`。

**修复**：
- 将测试查询改为返回字符串字面量：
  ```sql
  SELECT '1' AS val
  ```
- 这符合当前 `DatabaseConnection` 设计约束，未来若需支持非字符串类型，可以扩展 `QueryResult` 的表示方式。

## 4. 其他注意事项

- `testcontainers` 0.15 已弃用 `get_host_port`，应使用 `get_host_port_ipv4` 或 `get_host_port_ipv6`。
- 测试环境需要安装 Docker 并处于运行状态。
- 使用 `sqlx::any` 需要确保所有数据库驱动 feature（`mysql`、`postgres`）均已启用。
